development:
  sessions:
    default:
      database: stations
      hosts:
        - localhost:27017

production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>

test:
  sessions:
    default:
      database: stations_test
      hosts:
        - localhost:27017